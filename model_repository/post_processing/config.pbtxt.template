name: "post_processing"
backend: "python"
max_batch_size: $MAX_BATCH_SIZE
input [
{
    name: "logits"
    data_type: TYPE_FP32
    dims: [ $NUM_CLASSES ]
}
]

output [
  {
    name: "predicts"
    data_type: TYPE_STRING
    dims: [ -1 ]
  }
]

instance_group [{ kind: KIND_CPU }]